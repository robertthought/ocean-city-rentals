# Kamal Secrets
# These are loaded at deploy time - keep this file secure!

# Rails master key (from config/master.key)
RAILS_MASTER_KEY=$(cat config/master.key)

# GitHub Container Registry token
# Create at: github.com → Settings → Developer Settings → Personal Access Tokens
KAMAL_REGISTRY_PASSWORD=$GITHUB_TOKEN

# PostgreSQL password (generate a strong one)
POSTGRES_PASSWORD=$POSTGRES_PASSWORD

# Database URL for Rails
DATABASE_URL="postgresql://ocean_city_rentals:${POSTGRES_PASSWORD}@localhost:5432/ocean_city_rentals_production"
