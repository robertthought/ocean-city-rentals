<div class="max-w-7xl mx-auto">
  <div class="flex items-center justify-between mb-8">
    <h1 class="text-3xl font-bold text-gray-900">Property Submissions</h1>
    <div class="flex items-center gap-4">
      <%= link_to export_admin_property_submissions_path(format: :csv),
        class: "px-4 py-2 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition-colors font-semibold" do %>
        <svg class="w-5 h-5 inline-block mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
        </svg>
        Export CSV
      <% end %>
    </div>
  </div>

  <!-- Filters -->
  <div class="bg-white rounded-xl shadow mb-6 p-4">
    <div class="flex items-center gap-4">
      <span class="text-sm text-gray-500">Filter:</span>
      <%= link_to "All", admin_property_submissions_path,
        class: "px-3 py-1.5 rounded-lg text-sm font-semibold #{params[:status].blank? ? 'bg-blue-100 text-blue-700' : 'text-gray-600 hover:bg-gray-100'}" %>
      <%= link_to "Pending", admin_property_submissions_path(status: "pending"),
        class: "px-3 py-1.5 rounded-lg text-sm font-semibold #{params[:status] == 'pending' ? 'bg-orange-100 text-orange-700' : 'text-gray-600 hover:bg-gray-100'}" %>
      <%= link_to "Reviewed", admin_property_submissions_path(status: "reviewed"),
        class: "px-3 py-1.5 rounded-lg text-sm font-semibold #{params[:status] == 'reviewed' ? 'bg-green-100 text-green-700' : 'text-gray-600 hover:bg-gray-100'}" %>
    </div>
  </div>

  <!-- Submissions Table -->
  <div class="bg-white rounded-xl shadow overflow-hidden">
    <% if @submissions.any? %>
      <table class="w-full">
        <thead class="bg-gray-50 border-b border-gray-200">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Owner</th>
            <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Property</th>
            <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Details</th>
            <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Date</th>
            <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase tracking-wider">Status</th>
            <th class="px-6 py-3 text-right text-xs font-semibold text-gray-500 uppercase tracking-wider">Actions</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-200">
          <% @submissions.each do |submission| %>
            <tr class="hover:bg-gray-50">
              <td class="px-6 py-4">
                <div class="font-semibold text-gray-900"><%= submission.owner_name %></div>
                <div class="text-sm text-gray-500"><%= submission.email %></div>
                <% if submission.phone.present? %>
                  <div class="text-sm text-gray-500"><%= submission.phone %></div>
                <% end %>
              </td>
              <td class="px-6 py-4">
                <div class="font-semibold text-gray-900"><%= submission.property_address %></div>
                <div class="text-sm text-gray-500">
                  <%= [submission.city, submission.state, submission.zip].compact.join(", ") %>
                </div>
              </td>
              <td class="px-6 py-4">
                <% if submission.bedrooms.present? || submission.bathrooms.present? %>
                  <span class="text-sm text-gray-600">
                    <% if submission.bedrooms.present? %><%= submission.bedrooms %> BR<% end %>
                    <% if submission.bedrooms.present? && submission.bathrooms.present? %> / <% end %>
                    <% if submission.bathrooms.present? %><%= submission.bathrooms %> BA<% end %>
                  </span>
                <% else %>
                  <span class="text-gray-400 text-sm">Not specified</span>
                <% end %>
              </td>
              <td class="px-6 py-4 text-sm text-gray-500">
                <div><%= submission.created_at.strftime("%b %d, %Y") %></div>
                <div class="text-xs"><%= submission.created_at.strftime("%I:%M %p") %></div>
              </td>
              <td class="px-6 py-4">
                <% if submission.reviewed %>
                  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                    Reviewed
                  </span>
                <% else %>
                  <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-orange-100 text-orange-800">
                    Pending
                  </span>
                <% end %>
              </td>
              <td class="px-6 py-4 text-right">
                <div class="flex items-center justify-end gap-2">
                  <%= link_to admin_property_submission_path(submission),
                    class: "p-2 text-gray-400 hover:text-blue-600 hover:bg-blue-50 rounded",
                    title: "View" do %>
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                    </svg>
                  <% end %>
                  <a href="mailto:<%= submission.email %>" class="p-2 text-gray-400 hover:text-blue-600 hover:bg-blue-50 rounded" title="Email">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                    </svg>
                  </a>
                  <% if submission.phone.present? %>
                    <a href="tel:<%= submission.phone %>" class="p-2 text-gray-400 hover:text-green-600 hover:bg-green-50 rounded" title="Call">
                      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
                      </svg>
                    </a>
                  <% end %>
                  <% unless submission.reviewed %>
                    <%= button_to mark_reviewed_admin_property_submission_path(submission),
                      method: :post,
                      class: "p-2 text-gray-400 hover:text-green-600 hover:bg-green-50 rounded",
                      title: "Mark as Reviewed" do %>
                      <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                      </svg>
                    <% end %>
                  <% end %>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <!-- Pagination -->
      <div class="px-6 py-4 border-t border-gray-200 flex items-center justify-between">
        <p class="text-sm text-gray-500">
          Showing <%= @pagy.from %>-<%= @pagy.to %> of <%= number_with_delimiter(@pagy.count) %> submissions
        </p>
        <nav class="flex items-center gap-2">
          <% if @pagy.prev %>
            <%= link_to admin_property_submissions_path(page: @pagy.prev, status: params[:status]),
                class: "px-3 py-1.5 bg-white border border-gray-300 rounded hover:bg-gray-50 text-sm" do %>
              Previous
            <% end %>
          <% end %>
          <% if @pagy.next %>
            <%= link_to admin_property_submissions_path(page: @pagy.next, status: params[:status]),
                class: "px-3 py-1.5 bg-white border border-gray-300 rounded hover:bg-gray-50 text-sm" do %>
              Next
            <% end %>
          <% end %>
        </nav>
      </div>
    <% else %>
      <div class="px-6 py-12 text-center text-gray-500">
        <svg class="w-12 h-12 mx-auto mb-4 text-gray-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
        </svg>
        <p>No property submissions found</p>
      </div>
    <% end %>
  </div>
</div>
