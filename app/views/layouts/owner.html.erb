<!DOCTYPE html>
<html>
  <head>
    <title>Owner Dashboard | OCNJ Weekly Rentals</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="robots" content="noindex, nofollow">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 min-h-screen">
    <!-- Owner Header -->
    <header class="bg-gradient-to-r from-blue-800 to-blue-900 text-white shadow-lg">
      <div class="container mx-auto px-4 py-4">
        <div class="flex items-center justify-between">
          <div class="flex items-center gap-6">
            <%= link_to owner_root_path, class: "flex items-center gap-2 font-bold text-lg" do %>
              <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
              </svg>
              Owner Portal
            <% end %>

            <nav class="hidden md:flex items-center gap-1">
              <%= link_to "Dashboard", owner_root_path,
                class: "px-4 py-2 rounded-lg hover:bg-white/10 transition-colors #{request.path == owner_root_path ? 'bg-white/20' : ''}" %>
              <%= link_to "My Properties", owner_properties_path,
                class: "px-4 py-2 rounded-lg hover:bg-white/10 transition-colors #{request.path.start_with?('/owner/properties') ? 'bg-white/20' : ''}" %>
              <%= link_to "Claims", owner_claims_path,
                class: "px-4 py-2 rounded-lg hover:bg-white/10 transition-colors #{request.path.start_with?('/owner/claims') ? 'bg-white/20' : ''}" %>
            </nav>
          </div>

          <div class="flex items-center gap-4">
            <span class="text-sm text-blue-200 hidden sm:block"><%= current_owner.email %></span>
            <%= link_to "Logout", owner_logout_path, data: { turbo_method: :delete },
              class: "px-4 py-2 bg-white/10 hover:bg-white/20 rounded-lg text-sm font-semibold transition-colors" %>
          </div>
        </div>

        <!-- Mobile Nav -->
        <nav class="md:hidden flex items-center gap-2 mt-4 overflow-x-auto pb-2">
          <%= link_to "Dashboard", owner_root_path,
            class: "px-3 py-1.5 rounded-lg text-sm whitespace-nowrap #{request.path == owner_root_path ? 'bg-white/20' : 'hover:bg-white/10'}" %>
          <%= link_to "Properties", owner_properties_path,
            class: "px-3 py-1.5 rounded-lg text-sm whitespace-nowrap #{request.path.start_with?('/owner/properties') ? 'bg-white/20' : 'hover:bg-white/10'}" %>
          <%= link_to "Claims", owner_claims_path,
            class: "px-3 py-1.5 rounded-lg text-sm whitespace-nowrap #{request.path.start_with?('/owner/claims') ? 'bg-white/20' : 'hover:bg-white/10'}" %>
        </nav>
      </div>
    </header>

    <main class="container mx-auto px-4 py-8">
      <% if notice %>
        <div class="bg-green-50 border-l-4 border-green-500 p-4 mb-6 rounded-r-lg">
          <p class="text-green-700"><%= notice %></p>
        </div>
      <% end %>

      <% if alert %>
        <div class="bg-red-50 border-l-4 border-red-500 p-4 mb-6 rounded-r-lg">
          <p class="text-red-700"><%= alert %></p>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <footer class="bg-gray-800 text-gray-400 py-6 mt-auto">
      <div class="container mx-auto px-4 text-center text-sm">
        <p>&copy; <%= Date.current.year %> OCNJ Weekly Rentals. Owner Portal.</p>
        <%= link_to "Back to Main Site", root_path, class: "text-blue-400 hover:text-blue-300" %>
      </div>
    </footer>
  </body>
</html>
