<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; }
    .container { max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: #2563eb; color: white; padding: 20px; border-radius: 8px 8px 0 0; }
    .content { background: #f9fafb; padding: 20px; border: 1px solid #e5e7eb; }
    .property-card { background: white; padding: 15px; border-radius: 8px; margin-bottom: 20px; border: 1px solid #e5e7eb; }
    .lead-info { background: white; padding: 15px; border-radius: 8px; border: 1px solid #e5e7eb; }
    .label { font-size: 12px; color: #6b7280; text-transform: uppercase; margin-bottom: 4px; }
    .value { font-size: 16px; color: #111827; margin-bottom: 12px; }
    .message { background: #fffbeb; padding: 15px; border-radius: 8px; border-left: 4px solid #f59e0b; margin-top: 15px; }
    .footer { text-align: center; padding: 20px; color: #6b7280; font-size: 12px; }
    .btn { display: inline-block; background: #2563eb; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; margin-top: 15px; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1 style="margin: 0; font-size: 24px;">New Lead Received</h1>
      <p style="margin: 5px 0 0 0; opacity: 0.9;">OCNJ Weekly Rentals</p>
    </div>

    <div class="content">
      <div class="property-card">
        <div class="label">Property</div>
        <div class="value" style="font-weight: bold; font-size: 18px;"><%= @property.address %></div>
        <div class="value"><%= @property.city %>, <%= @property.state %> <%= @property.zip %></div>
        <a href="<%= property_url(@property) %>" class="btn">View Property</a>
      </div>

      <div class="lead-info">
        <h2 style="margin-top: 0; color: #111827;">Contact Information</h2>

        <div class="label">Name</div>
        <div class="value"><%= @lead.name %></div>

        <div class="label">Email</div>
        <div class="value"><a href="mailto:<%= @lead.email %>"><%= @lead.email %></a></div>

        <% if @lead.phone.present? %>
          <div class="label">Phone</div>
          <div class="value"><a href="tel:<%= @lead.phone %>"><%= @lead.phone %></a></div>
        <% end %>

        <% if @lead.message.present? %>
          <div class="message">
            <div class="label">Message</div>
            <div style="color: #111827;"><%= simple_format(@lead.message) %></div>
          </div>
        <% end %>
      </div>

      <p style="margin-top: 20px; color: #6b7280; font-size: 14px;">
        <strong>Submitted:</strong> <%= @lead.created_at.strftime("%B %d, %Y at %I:%M %p") %><br>
        <strong>Lead Type:</strong> <%= @lead.lead_type&.titleize || "General Inquiry" %>
      </p>
    </div>

    <div class="footer">
      <p>This email was sent from OCNJ Weekly Rentals<br>
      <a href="https://ocnjweeklyrentals.com">ocnjweeklyrentals.com</a></p>
    </div>
  </div>
</body>
</html>
